# Expo Managed Workflow Scaffold

**Branch:** main  
**Base branch:** main  
**PR strategy:** one (force-pushed directly to main)  
**Skill-retro:** yes  
**Timestamp:** 2026-05-06

## Stories

- [x] Story 1 — Core scaffold (44ea408, 8100a1a)
- [x] Story 2 — EAS Build (b30bd88)
- [x] Story 3 — CI workflow (47f6c6f)

## What was delivered

- `package.json` — Expo 54 + jest-expo testing setup, iOS-only scripts
- `app.json` — managed workflow config: `dog.gnar.hlyst` bundle ID, audio background mode, portrait only
- `App.tsx` / `index.ts` — entry point
- `tsconfig.json` / `babel.config.js` — TypeScript + Expo Babel
- `.gitignore` — excludes `ios/` and `android/` (generated by `expo prebuild`)
- `assets/` — placeholder icons
- `__tests__/App.test.tsx` — smoke test (module export check)
- `eas.json` — development (simulator), preview, production build profiles
- `.github/workflows/ci.yml` — test + typecheck jobs on Node 22

## Next steps

- Run `eas build:configure` to link an EAS project ID (writes to `app.json`)
- Replace placeholder assets with real icons
- Add `@testing-library/react-native` when writing component tests (needs `--legacy-peer-deps` or version pin with react@19.1.0)
